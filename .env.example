DEBUG=True
SECRET_KEY=change-me-for-production
# ALLOWED_HOSTS=localhost,127.0.0.1,your-public-ip
# عند DEBUG=False والدخول من IP:port — لتجنب أخطاء CSRF:
# CSRF_TRUSTED_ORIGINS=http://your-public-ip:8000
# PostgreSQL (leave unset to use SQLite):
# DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DBNAME
CAFE_NAME_AR=مقهى النموذج
CAFE_NAME_EN=Demo Café
